const Underline = () => (
  <div
    id="navbarUndeline"
    className="bg-white h-0.5 mt-1 absolute transition-all duration-500"
    style={{
      width: `100%`,
    }}
  />
);

export default Underline;
